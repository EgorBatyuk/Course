import 'package:flutter/material.dart';

class Ten extends StatelessWidget {
  const Ten({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Формулы'),
      ),
      body: Text('Получилось'),
    );
  }
}